# Results

Raw experiment results are stored here but gitignored (they are regenerable).

To regenerate all results:
```bash
python experiments/run_all.py
```

Summary files (`*_summary.csv`) are committed for reference.
